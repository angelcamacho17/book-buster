import { SetHeaderHeightDirective } from './set-header-height.directive';

describe('SetHeaderHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new SetHeaderHeightDirective();
    expect(directive).toBeTruthy();
  });
});
