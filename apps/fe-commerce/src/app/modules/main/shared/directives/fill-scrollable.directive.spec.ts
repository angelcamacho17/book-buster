import { FillScrollableDirective } from './fill-scrollable.directive';

describe('FillScrollableDirective', () => {
  it('should create an instance', () => {
    const directive = new FillScrollableDirective();
    expect(directive).toBeTruthy();
  });
});
